# 实验验证记录

## 已验证：2026-09-17

- GitHub Actions -> 本机 Runner 连通测试成功：运行 35201703061。
- Codex CLI 0.151.0 使用已有 ChatGPT 登录、HTTPS 传输，返回结构化结果成功。
- 本地真实 Agent 集成测试：第一轮提出澄清问题并持久化；测试驱动在新进程提供反馈，恢复历史后生成中文待办应用。
- 实际浏览器首次拒绝非扁平的测试计划；将真实错误返回 Agent 后，仅修复计划格式，保留全部 23 个步骤和断言。
- 再次浏览器执行通过：创建两条任务、完成其中一条、刷新后保留、切换未完成/已完成/全部筛选、删除，生成桌面和手机截图。
- 10 项单元测试通过，覆盖非所有者拒绝、非命令不触发、重复事件不重复执行、fork PR 在执行前拒绝、持久化、预览文件边界、远端分支并发变更拒绝覆盖。
- Node 语法、工作流 YAML 和 Git 空白校验通过。

## 尚未验证

完整的 GitHub Issue -> Agent 提问 -> 用户评论 -> 续跑 -> Pages 发布 -> 截图回写，必须在默认分支激活后再实测。不能把本地集成测试等同于该链路已通过。

本地两轮反馈由测试驱动提供，非真实用户验收。浏览器路径由实现方编写，不是独立业务验收。静态预览不包含真实后端或认证。

原始模型日志、Session 和截图保留在本机 Runner 的 sessions/local-e2e 下，不提交个人运行历史。
